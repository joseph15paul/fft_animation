Controls:
    scroll - zoom
    w,a,s,d - pan
    0 - halve the number of samples taken
    9 - double the number of samples taken
    b - slow down
    n - speed up

Usage:
    sh ./buildNrun.sh "path/to/SVG"

Example:
    sh ./buildNrun.sh ./Input/butterfly.svg
